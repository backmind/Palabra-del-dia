[project]
name = "palabra-del-dia"
version = "0.1.0"
description = "Scrapes La Palabra del Día from elcastellano.org and generates an Atom feed"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "beautifulsoup4>=4.13",
    "lxml>=5.0",
]
